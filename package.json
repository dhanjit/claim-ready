{
  "name": "claim-ready",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:rules": "node scripts/build-rules.mjs",
    "dev": "npm run build:rules && wrangler dev",
    "test": "npm run build:rules && vitest run",
    "deploy": "npm run build:rules && wrangler deploy"
  },
  "devDependencies": {
    "js-yaml": "4.1.0",
    "typescript": "5.6.3",
    "vitest": "2.1.9",
    "wrangler": "4.14.1"
  }
}
